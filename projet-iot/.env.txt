# Navidrome server URL (overrides navidrome-config.json serverUrl)
NAVIDROME_URL=http://100.98.192.1:4533
NAVIDROME_USER=Merzouz
NAVIDROME_PASS=Merlin.2016

# Credentials for projet-web backend (used to fetch authenticated GPS points)
BACKEND_USER=jean.dupont@example.com
BACKEND_PASS=Password123!
# GPS serial device path for NEO-M8N (override autodetection if needed)
GPS_SERIAL_PATH=/dev/ttyACM0
