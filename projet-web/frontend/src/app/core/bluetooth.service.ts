import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class BluetoothService {
  // Placeholder Bluetooth service for device discovery and connections
  isEnabled(): boolean {
    return false;
  }

  scan() {
    // TODO: implement scan
  }
}
